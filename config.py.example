import os

# ===== 基础配置 =====
# 你的 GitHub 账号名
GITHUB_USER = "your_github_username"

# 你的 GitHub 仓库名（用于生成站点 URL）
REPO_NAME = "ZhiHuArchive"

# 站点基础 URL，通常不需要修改
# 格式: https://{GITHUB_USER}.github.io/{REPO_NAME}
BASE_URL = f"https://{GITHUB_USER}.github.io/{REPO_NAME}"

# 站点标题
SITE_TITLE = "知乎备份"

# 站点描述（用于搜索引擎和 RSS）
SITE_DESCRIPTION = "知乎文章和回答备份目录"
